# requirements.txt — Dependencias del proyecto GameVault
# Para instalarlas todas, ejecuta en la terminal:
#   pip install -r requirements.txt

# Flask: el microframework web que usamos como servidor backend
# La versión 3.x es la más reciente y estable (2024)
Flask==3.1.1
gunicorn==21.2.0